export default {
    title: 'React Memo'
}


export const Filter = (props:any) => {
    return (
        <div>{props.count}</div>
    )
}

export const Table = (props:any) => {
    return (
        <div>{props.count}</div>
    )
}
